import { buildPageMetadata } from '@/utils/seoConfig';

export const metadata = buildPageMetadata({
  title: 'AI 问答',
  description: '墨子 AI 问答：用自然语言查询币种行情、板块与市场分析。',
  path: '/ai',
});

export default function AiPage() {
  return null;
}
