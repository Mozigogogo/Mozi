# 若未使用 Dockerfile：告诉 Nixpacks 这是纯 Node，不要跑 next build
[variables]
NIXPACKS_NODE_VERSION = "20"

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["echo 'telegram bot: no compile step'"]

[start]
cmd = "npm start"
