---
description: 开发任务必须走 dev-expert 六步闭环；新会话先恢复项目记忆
alwaysApply: true
---

# Mozi 开发工作流（dev-expert）

编码 / 改需求 / 修 Bug / 重构等**会改仓库**的任务，按本仓库 `.cursor/dev-expert-1.0.52` 技能流程执行；不要跳过验证与复盘。

## 新会话（强制）

1. 先读取并遵循：`.cursor/dev-expert-1.0.52/SKILL.md`（至少 Step 0–1 与六步闭环）。
2. 按技能要求恢复 `.ai-memory/`（若存在）：当日 `topics.md`、最近 `session_memory_*.jsonl`、需要时再读 `project_memory.md` / `handoff.md`。
3. 用户说「继续 / 下一步 / 接着做」时，先对齐上次主线与阻塞点，再动手。

## 执行要求

- **复杂任务**：先给方案、影响范围、验证路径；涉及库写入 / 权限 / 生产配置须先确认。
- **简单任务**：可快速执行，但仍须给出可核对的验证结果。
- **纯问答 / 只读**：不必强行走完整闭环，也不可假装已改代码。
- 未经验证的改动不要当作已交付。

## 显式调用（可选但推荐）

用户可用 `@.cursor/dev-expert-1.0.52` 或 `@code-generation` / `@bug-diagnosis` 等指定子技能；未指定时按意图路由，流程门控仍生效。
