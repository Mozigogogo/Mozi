# 必填：@BotFather 获取
BOT_TOKEN=

# Mini App / 站点根地址（与 BotFather 中 Mini App 域名一致时 web_app 按钮最稳）
APP_URL=https://moziinnovations-production.up.railway.app

# 自建 API 根地址（不含路径末尾 /）；未设置时默认为 https://moziinnovations.com
# API_BASE_URL=https://moziinnovations.com

# 机器人 @ 用户名，不要带 @；与 Telegram 上实际 bot 名一致
BOT_USERNAME=Moziinovations_bot

# 可选：/start 与 /alert 卡片图 URL
# ALERT_CARD_IMAGE=https://example.com/card.jpg

# /ai：流式分析 POST 完整 URL（可选；默认直连 mozibackend-production …/api/v1/analyze/stream，契约见 bot/lib/apis.js）
# AI_BACKEND_URL=https://your-api.example.com/api/v1/analyze/stream

# /chat：流式对话 POST 完整 URL（可选；默认 ${API_BASE_URL}/ai/chat/stream）
# AI_CHAT_BACKEND_URL=https://your-api.example.com/ai/chat/stream
# 可选：Authorization: Bearer …
# AI_BACKEND_SECRET=
# 底部展示积分数；后端也可在 JSON 里返回 pointsCost 覆盖
# AI_POINTS_COST=50
